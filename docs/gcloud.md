# gcloud

## cli

```shell
gcloud compute ssh --project [PROJECT_ID] --zone [ZONE] [INSTANCE_NAME]
gcloud compute ssh --project zomi-server --zone us-central1-a home

gcloud compute ssh [INSTANCE-NAME]
